# 07. 데이터 모델 정의서

## 1. 문서 개요
| 항목 | 내용 |
|---|---|
| 문서명 | 멀티 프로젝트 챗봇 플랫폼 데이터 모델 정의서 |
| 작성일 | 2026-06-22 |
| 버전 | v2.0 (Multi-Tenant 확장판) |
| 설계 원칙 | - 모든 데이터베이스 조회 쿼리에 `workspace_id` 조건이 필수로 포함될 수 있도록 논리적 외래키 격리 구조를 강제합니다.<br>- Vector 및 Graph 데이터 저장용 `pgvector` 확장을 사용합니다. |

---

## 2. 권한 및 테넌트(Workspace) 핵심 모델

### `workspaces` (프로젝트/테넌트 기본 정보)
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | Workspace 고유 ID | |
| name | VARCHAR(100) | | | Y | 프로젝트 명 (예: NETZERO, DPPA) | |
| description | TEXT | | | N | 프로젝트 설명 | |
| is_active | BOOLEAN | | | Y | 활성화 상태 | 기본값 TRUE |
| created_at | TIMESTAMPTZ | | | Y | 생성 일시 | |

### `workspace_users` (Workspace 구성원 및 권한 맵핑)
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 매핑 고유 ID | |
| workspace_id | UUID | | O | Y | 참조할 Workspace ID | workspaces(id) |
| user_email | VARCHAR(255) | | | Y | 사내망/SSO 이메일 | 시스템 통합 계정 식별 |
| role | VARCHAR(50) | | | Y | 역할 (W-Admin, K-Manager, Operator, Viewer) | |
| created_at | TIMESTAMPTZ | | | Y | 등록 일시 | |

---

## 3. 챗봇 설정 및 프롬프트 모델

### `bot_settings` (프로젝트별 챗봇 환경 설정)
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 설정 고유 ID | |
| workspace_id | UUID | | O | Y | 종속 Workspace | 1:1 관계 |
| bot_name | VARCHAR(100) | | | Y | 위젯 노출 챗봇명 | |
| welcome_message| TEXT | | | Y | 시작 인사말 | |
| primary_color | VARCHAR(20) | | | Y | 브랜드 테마 색상 (Hex) | UI 커스텀 |
| created_at | TIMESTAMPTZ | | | Y | 생성 일시 | |

### `system_prompts` (버전 관리 프롬프트)
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 프롬프트 고유 ID | |
| workspace_id | UUID | | O | Y | 종속 Workspace | |
| prompt_text | TEXT | | | Y | 시스템 프롬프트(지시어) 본문 | |
| version | INTEGER | | | Y | 프롬프트 버전 관리 | 자동 증가 |
| is_active | BOOLEAN | | | Y | 현재 배포(적용) 여부 | Workspace당 1개만 활성 |

### `suggested_questions` (추천 질문 관리)
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 고유 ID | |
| workspace_id | UUID | | O | Y | 종속 Workspace | |
| question_text | VARCHAR(500) | | | Y | 노출될 추천 질문 문구 | |
| sort_order | INTEGER | | | Y | 노출 순서 | |

---

## 4. 지식 파이프라인 데이터 모델 (RAG / Graph)

### `sources` (업로드된 원본 문서)
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 소스 문서 ID | |
| workspace_id | UUID | | O | Y | 종속 Workspace | |
| file_name | VARCHAR(500) | | | Y | 업로드된 파일명 | |
| file_size | BIGINT | | | Y | 파일 크기(Bytes) | |
| status | VARCHAR(20) | | | Y | 인덱싱 상태 (READY, PROCESSING, DONE, FAILED) | |

### `chunks` (파싱된 텍스트 청크 - Vector)
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 청크 ID | |
| workspace_id | UUID | | O | Y | 종속 Workspace | 데이터 격리 필수 |
| source_id | UUID | | O | Y | 원본 문서 FK | |
| chunk_text | TEXT | | | Y | 파싱된 텍스트 본문 | |
| embedding | VECTOR(1536) | | | N | 임베딩 벡터값 (pgvector) | HNSW 인덱스 적용 |

### `entities` & `relations` (Graph 추출 데이터)
*GraphRAG의 핵심인 도메인 특화 관계도(Knowledge Graph) 저장 테이블.*

**`entities`**
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 엔티티 고유 ID | |
| workspace_id | UUID | | O | Y | 종속 Workspace | |
| name | VARCHAR(255) | | | Y | 엔티티 명 (예: DPPA정산서) | |
| type | VARCHAR(100) | | | Y | 종류 (메뉴, 배출원, 지표 등) | |

**`relations`**
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 관계 고유 ID | |
| workspace_id | UUID | | O | Y | 종속 Workspace | |
| head_entity_id | UUID | | O | Y | 주체 엔티티 (Source) | |
| tail_entity_id | UUID | | O | Y | 객체 엔티티 (Target) | |
| relation_type | VARCHAR(100) | | | Y | 관계 정의 (예: 산정기준이다) | |
| description | TEXT | | | N | LLM이 파악한 관계 요약문 | |

---

## 5. 대화 및 운영 기록 모델

### `chat_sessions` & `chat_messages`
| 테이블명 | 컬럼명 | 타입 | 설명 | 비고 |
|---|---|---|---|---|
| `chat_sessions` | id (PK) | UUID | 세션 고유 ID | |
| | workspace_id(FK)| UUID | 특정 프로젝트에서의 대화 | |
| | title | VARCHAR(255)| 대화 주제 요약(LLM 자동 생성) | |
| `chat_messages` | id (PK) | UUID | 개별 메시지 ID | |
| | session_id(FK) | UUID | 소속 세션 ID | |
| | role | VARCHAR(20) | 발화자 (user, assistant) | |
| | content | TEXT | 질문 또는 답변 본문 | |
| | feedback_score| INTEGER | 사용자가 남긴 평가 (-1, 0, 1) | 좋아요/싫어요 |
| | is_fallback | BOOLEAN | 답변 실패(미응답) 처리 여부 | |

### `evidences` (답변 출처 증빙 맵핑)
*답변 시 활용된 RAG 문서를 맵핑하여 위젯에 출처 칩 형태로 제공하기 위함.*
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| message_id | UUID | O | O | Y | 답변 메시지 ID | |
| chunk_id | UUID | O | O | Y | 참조한 청크 ID | |
| relevance_score| FLOAT | | | Y | 벡터/그래프 검색 유사도(신뢰도) | |

### `audit_logs` (관리자 보안 감사 로그)
| 컬럼명 | 타입 | PK | FK | Not Null | 설명 | 비고 |
|---|---|:---:|:---:|:---:|---|---|
| id | UUID | O | | Y | 로그 고유 ID | |
| workspace_id | UUID | | O | Y | 변경이 발생한 Workspace | |
| user_email | VARCHAR(255) | | | Y | 행위자 (관리자 이메일) | |
| action | VARCHAR(100) | | | Y | 발생 액션 (예: PROMPT_UPDATE) | |
| details | JSONB | | | N | 수정 전/후 값 등 상세 내역 | 보안 증빙 |
