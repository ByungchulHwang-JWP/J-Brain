# [TE-D264] J-Brain 사용자 테스트(UAT) 상세 결과서

**프로젝트명:** J-Brain AI 챗봇 통합 플랫폼 구축  
**문서 ID:** 사업ID-TE-D264-테스트결과서-v3.0  
**작성일:** 2026-07-04  
**문서 목적:** 본 문서는 사전 지식이 없는 사용자도 수행 가능한 극도로 상세한(Step-by-step) 테스트 케이스를 제공하며, SI 표준 테스트결과서(TE-D264) 양식에 맞추어 작성되었습니다.

---

## 📌 1. 테스트 환경 및 사전 조건
- **접속 URL:** `http://localhost:5174` (프론트엔드 로컬 서버)
- **테스트 계정:** 최고 관리자 (admin)
- **준비물:** 바탕화면에 아래 4개의 테스트 소스 파일을 미리 준비합니다.
  - `03_J-Brain_운영_매뉴얼_및_가이드.md`
  - `04_탄소중립플랫폼_통합_매뉴얼.md`
  - `05_DPPA_직접전력거래_가이드.md`
  - `06_GIMS_온실가스_인벤토리_매뉴얼.md`

---

## 🚀 2. 상세 테스트 케이스 및 결과 (J-Brain 플랫폼 자체 챗봇 구축)

| TC ID | 기능 분류 | 화면/기능명 | 테스트 수행 절차 (Step-by-Step) | 예상 결과 | 결과(P/F) | 비고 |
| :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **JB-001** | 로그인 | 관리자 로그인 | 1. 브라우저에서 `http://localhost:5174/login` 접속<br>2. ID 입력칸에 관리자 아이디 입력<br>3. Password 입력칸에 비밀번호 입력<br>4. [로그인] 버튼 클릭 | 로그인 성공 후 '워크플로우 대시보드' 화면이 노출된다. | `[ ]` | |
| **JB-002** | 1단계 | 신규 프로젝트 생성 | 1. 좌측 메뉴에서 [관리 기능] > [프로젝트 관리] 클릭<br>2. 우측 상단의 [프로젝트 생성] 버튼 클릭<br>3. 프로젝트명: `J-Brain` 입력<br>4. 설명: `J-Brain 사용 안내 챗봇` 입력<br>5. [저장] 버튼 클릭 | 성공 메시지와 함께 목록에 'J-Brain' 프로젝트가 생성된다. | `[ ]` | |
| **JB-003** | 공통 | 프로젝트 선택 | 1. 화면 최상단(헤더)의 '프로젝트 선택' 콤보박스 클릭<br>2. 목록에서 방금 생성한 `J-Brain`을 클릭하여 선택 | 화면 상단에 'J-Brain'이 고정 표시되며, 이후 모든 데이터가 해당 프로젝트 기준으로 조회된다. | `[ ]` | |
| **JB-004** | 2단계 | Source 업로드 | 1. 좌측 메뉴 [관리 기능] > [Source 관리] 클릭<br>2. 우측 상단 [파일 업로드] 버튼 클릭<br>3. `03_J-Brain_운영_매뉴얼_및_가이드.md` 파일 선택<br>4. [업로드] 버튼 클릭 | 파일이 목록에 추가되고 상태가 `Processing` 또는 `Pending`으로 표시된다. | `[ ]` | |
| **JB-005** | 2단계 | 벡터화 상태 확인 | 1. 좌측 메뉴 [관리 기능] > [벡터화 작업 현황] 클릭<br>2. 키보드 F5키(새로고침)를 간헐적으로 누르며 대기 | 작업 상태(Status)가 최종적으로 `SUCCESS`, 진행률(Progress)이 `100%`로 변경된다. | `[ ]` | |
| **JB-006** | 2단계 | 검색 테스트 | 1. 좌측 메뉴 [관리 기능] > [검색 테스트] 클릭<br>2. 검색창에 `J-Brain은 무엇인가요?` 입력<br>3. 우측 [검색] 버튼 클릭 | 하단 결과 화면에 J-Brain 매뉴얼 문서에서 추출한 RAG 청크 데이터가 정상적으로 표시된다. | `[ ]` | |
| **JB-007** | 3단계 | Action 등록 | 1. 좌측 메뉴 [관리 기능] > [Action 관리] 클릭<br>2. [Action 등록] 버튼 클릭<br>3. ID: `ACT-JB-SEARCH`, 이름: `문서 검색` 입력<br>4. Action 유형: `SEARCH_DOC` 선택 후 [저장] 클릭 | Action 목록에 `ACT-JB-SEARCH` 가 등록된다. | `[ ]` | |
| **JB-008** | 3단계 | Intent 등록 | 1. 좌측 메뉴 [관리 기능] > [Intent 관리] 클릭<br>2. [Intent 등록] 버튼 클릭<br>3. ID: `INT-JB-INTRO`, 이름: `J-Brain 소개` 입력<br>4. 연결할 Action 드롭다운에서 `ACT-JB-SEARCH` 선택 후 [저장] 클릭 | Intent 목록에 `INT-JB-INTRO`가 추가된다. | `[ ]` | |
| **JB-009** | 4단계 | Example 추가 | 1. Intent 관리 화면에서 `INT-JB-INTRO` 행을 클릭<br>2. 하단 'Example 추가' 영역 입력칸에 `J-Brain이 뭔가요?` 입력 후 [추가] 클릭<br>3. `시스템 설명해줘` 입력 후 [추가] 클릭<br>4. 하단 [저장] 버튼 클릭 | 해당 Intent 내에 2개의 예시 발화(Example) 문장이 묶여서 정상 저장된다. | `[ ]` | |
| **JB-010** | 6단계 | FAQ 직접 등록 | 1. 좌측 메뉴 [관리 기능] > [FAQ 관리] 클릭<br>2. [FAQ 등록] 버튼 클릭<br>3. 질문: `미응답 분석 기능이 뭔가요?`<br>4. 답변: `답변 실패 내역을 분석하는 기능입니다.` 입력<br>5. 'Pack Export 반영' 체크박스 선택 후 [저장] 클릭 | FAQ 목록에 추가되며, Pack 반영 상태가 켜짐(On)으로 표시된다. | `[ ]` | |
| **JB-011** | 10단계 | Pack Draft 생성 | 1. 좌측 메뉴 [관리 기능] > [Pack Builder] 클릭<br>2. 화면 상단의 [Draft 생성] 버튼 클릭 | Intent, Action, FAQ 데이터베이스 내역이 취합되어 수치로 표시된다. | `[ ]` | |
| **JB-012** | 10단계 | Pack Export | 1. Draft 화면에서 하단의 [Export] 버튼 클릭 | ZIP 생성이 완료되며 Export 성공 메시지가 표시된다. | `[ ]` | |
| **JB-013** | 11단계 | Pack Import | 1. 좌측 메뉴 [관리 기능] > [Pack Repository] 클릭<br>2. Export 이력에서 가장 최근 항목의 [Import] 버튼 클릭 | 하단 Runtime Pack Store 목록에 해당 Pack 버전이 반입되어 추가된다. | `[ ]` | |
| **JB-014** | 11단계 | 승인 및 활성화 | 1. Runtime Pack Store 목록 우측의 [Approve] 버튼 클릭<br>2. 상태 변경 확인 후, 이어서 [Activate] 버튼 클릭 | Pack의 상태가 `Active`로 최종 변경되며 런타임 엔진에 즉각 반영된다. | `[ ]` | |
| **JB-015** | 12단계 | FAQ 질의 테스트 | 1. 좌측 메뉴 [운영/분석] > [챗봇 대화 테스트] 클릭<br>2. 하단 채팅창에 `미응답 분석 기능이 뭔가요?` 입력 후 엔터키 입력 | RAG 벡터 검색 없이, 등록된 FAQ를 기반으로 챗봇이 즉각적으로 텍스트 답변을 출력한다. | `[ ]` | |
| **JB-016** | 12단계 | RAG 질의 테스트 | 1. 챗봇 대화창에 `J-Brain 챗봇 구축 12단계에 대해 알려줘.` 입력 후 엔터키 입력 | 약 3초 뒤, 업로드된 03번 매뉴얼을 기반으로 LLM이 요약 생성한 12단계 워크플로우 텍스트가 반환된다. | `[ ]` | |
| **JB-017** | 12단계 | Intent 검증 | 1. 방금 출력된 답변 하단(또는 우측 디버깅 패널)에 표시된 Intent 매칭 결과 확인 | 매칭된 Intent ID가 관리자가 설정한 `INT-JB-INTRO` 로 정확하게 표시된다. | `[ ]` | |

---

## 🌍 3. 탄소중립플랫폼 챗봇 테스트 (다중 지식 응용)

동일 시스템에서 프로젝트를 분리하여 전혀 다른 도메인(탄소중립, DPPA, GIMS) 문서를 다중 주입하고 챗봇을 구축하는 응용 테스트입니다. 이 단계 역시 초보자가 한 단계씩 따라 할 수 있도록 극도로 상세히 분류되었습니다.

| TC ID | 기능 분류 | 화면/기능명 | 테스트 수행 절차 (Step-by-Step) | 예상 결과 | 결과(P/F) | 비고 |
| :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **CN-001** | 프로젝트 | 신규 프로젝트 세팅 | 1. 좌측 메뉴 [관리 기능] > [프로젝트 관리] 클릭<br>2. [프로젝트 생성] 버튼 클릭<br>3. 프로젝트명: `탄소중립 플랫폼` 입력 후 [저장] 클릭<br>4. 최상단 헤더 '프로젝트 선택' 드롭다운에서 방금 생성한 `탄소중립 플랫폼`을 클릭하여 전환 | 상단 프로젝트명이 '탄소중립 플랫폼'으로 변경되며 독립된 환경으로 진입한다. | `[ ]` | |
| **CN-002** | 2단계 | 지식 업로드 (코어) | 1. 좌측 메뉴 [관리 기능] > [Source 관리] 클릭<br>2. [파일 업로드] 버튼 클릭<br>3. `04_탄소중립플랫폼_통합_매뉴얼.md` 파일 선택 후 [업로드] 클릭 | 04번 파일이 목록에 추가되고 `Pending` 상태로 표시된다. | `[ ]` | |
| **CN-003** | 2단계 | 지식 업로드 (DPPA) | 1. 동일한 화면에서 다시 [파일 업로드] 버튼 클릭<br>2. `05_DPPA_직접전력거래_가이드.md` 파일 선택 후 [업로드] 클릭 | 05번 파일이 목록에 추가된다. | `[ ]` | |
| **CN-004** | 2단계 | 지식 업로드 (GIMS) | 1. 동일한 화면에서 다시 [파일 업로드] 버튼 클릭<br>2. `06_GIMS_온실가스_인벤토리_매뉴얼.md` 파일 선택 후 [업로드] 클릭 | 06번 파일이 목록에 추가된다. (총 3개 문서 대기중) | `[ ]` | |
| **CN-005** | 2단계 | 일괄 벡터화 대기 | 1. 좌측 메뉴 [관리 기능] > [벡터화 작업 현황] 클릭<br>2. 키보드 F5키(새로고침)를 누르며 3개 파일이 모두 100% 진행될 때까지 대기 | 3개의 파일 상태가 모두 `SUCCESS`로 변경된다. (다중 지식 주입 완료) | `[ ]` | |
| **CN-006** | 3단계 | Action 등록 | 1. 좌측 메뉴 [관리 기능] > [Action 관리] 클릭<br>2. [Action 등록] 버튼 클릭<br>3. ID: `ACT-CN-SEARCH`, 이름: `탄소중립 지식검색` 입력<br>4. Action 유형: `SEARCH_DOC` 선택 후 [저장] 클릭 | Action 목록에 `ACT-CN-SEARCH` 가 등록된다. | `[ ]` | |
| **CN-007** | 3단계 | Intent 등록 | 1. 좌측 메뉴 [관리 기능] > [Intent 관리] 클릭<br>2. [Intent 등록] 버튼 클릭<br>3. ID: `INT-CN-INFO`, 이름: `탄소 정보 질의` 입력<br>4. Action 연결 드롭다운에서 `ACT-CN-SEARCH` 선택 후 [저장] 클릭 | Intent 목록에 `INT-CN-INFO`가 추가된다. | `[ ]` | |
| **CN-008** | 4단계 | Example 추가 | 1. Intent 관리 화면에서 `INT-CN-INFO` 행 클릭<br>2. 하단 'Example 추가' 영역에 `탄소중립플랫폼 알려줘` 입력 후 [추가] 클릭<br>3. `DPPA가 뭐야?` 입력 후 [추가] 클릭<br>4. `온실가스 명세서 어떻게 작성해?` 입력 후 [추가] 클릭<br>5. 하단 [저장] 버튼 클릭 | 탄소중립, DPPA, GIMS를 아우르는 3개의 대표 예시 발화 문장이 정상 저장된다. | `[ ]` | |
| **CN-009** | 10단계 | Pack Draft 및 Export | 1. 좌측 메뉴 [관리 기능] > [Pack Builder] 클릭<br>2. 상단 [Draft 생성] 버튼 클릭<br>3. 하단 [Export] 버튼 클릭 | ZIP 생성이 완료되며 Export 성공 메시지가 표시된다. | `[ ]` | |
| **CN-010** | 11단계 | Pack Import 및 승인 | 1. 좌측 메뉴 [관리 기능] > [Pack Repository] 클릭<br>2. 가장 최근 내역 우측의 [Import] 버튼 클릭<br>3. 하단 Runtime Pack Store 목록에서 해당 Pack의 [Approve(승인)] 클릭<br>4. 상태 변경 확인 후 [Activate(활성화)] 클릭 | 탄소중립 챗봇 엔진 구축이 최종 완료되며 상태가 `Active`로 변경된다. | `[ ]` | |
| **CN-011** | 12단계 | 챗봇 테스트 (코어) | 1. 좌측 메뉴 [운영/분석] > [챗봇 대화 테스트] 클릭<br>2. 상단 프로젝트가 `탄소중립 플랫폼`인지 확인<br>3. 챗봇 채팅창에 `탄소중립플랫폼은 어떤 시스템인가요?` 입력 후 엔터키 입력 | 04번 문서를 참조하여 플랫폼의 핵심 목표 및 제공 서비스(GIMS, DPPA, OCMS)를 종합적으로 답변한다. | `[ ]` | |
| **CN-012** | 12단계 | 챗봇 테스트 (DPPA) | 1. 채팅창에 `제3자 PPA와 직접 PPA의 차이점이 뭐야?` 입력 후 엔터키 입력 | 05번 문서를 참조하여 한전 중개 유무 및 직접 계약의 차이점에 대해 상세히 비교 설명한다. | `[ ]` | |
| **CN-013** | 12단계 | 챗봇 테스트 (GIMS) | 1. 채팅창에 `온실가스 배출량 명세서는 어떻게 작성하나요?` 입력 후 엔터키 입력 | 06번 문서를 참조하여 전기, 가스 등의 활동자료 입력 및 배출계수 적용을 통한 자동 생성 절차를 답변한다. | `[ ]` | |
| **CN-014** | 12단계 | 챗봇 테스트 (교차) | 1. 채팅창에 `Scope 1, 2, 3의 차이를 각각 설명해 줄래?` 입력 후 엔터키 입력 | 06번 문서(GIMS 매뉴얼)를 참조하여 직접/간접/기타 간접 배출량의 정의와 예시를 명확히 구분하여 답변한다. | `[ ]` | |

---

### [결재 / 확인란]
* **테스트 수행자:** (서명) _________________
* **테스트 수행일:** 2026. __. __.
* **프로젝트 PM:** (서명) _________________
