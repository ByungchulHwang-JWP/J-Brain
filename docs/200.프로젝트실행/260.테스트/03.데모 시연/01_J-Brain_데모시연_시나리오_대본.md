# J-Brain Intent Factory 데모 시연 시나리오 대본

> **시연 목적:** 폐쇄망 환경에서도 외부 LLM 없이 안전하게 동작하는 공통 챗봇 플랫폼 J-Brain의 핵심 가치와 'Intent Factory 6단계 워크플로우'를 선보입니다.
> **발표자(Presenter):** PM (또는 시연 담당자)
> **소요 시간:** 약 10~15분

---

## [사전 준비]
- **데이터 상태:** `cleanse_demo_data.py`를 통해 모든 프로젝트/지식/의도 데이터가 `TRUNCATE(RESTART IDENTITY)` 된 완벽한 백지 상태
- **백업 파일:** `01_백업데이터/` 폴더 내에 `J-Brain_Manual.txt`, `03_J-Brain_운영_매뉴얼_및_가이드.md` 준비 확인
- **서버 구동:** 로컬에서 `start_dev.sh` 실행 상태 확인 (`localhost:5174` 띄워두기)

---

## 1단계: 프로젝트 생성 (00:00 ~ 02:00)

**[화면 액션]**
- `localhost:5174` 로그인 (미리 로그인된 화면에서 시작 권장)
- 텅 빈 대시보드 화면을 보여줌
- **[신규 프로젝트 생성]** 버튼 클릭 (프로젝트명: `탄소중립 플랫폼 데모`, 설명: `폐쇄망 시연용`)

**🗣️ 발표 멘트:**
> "안녕하십니까, 지금부터 J-Brain 플랫폼의 데모 시연을 시작하겠습니다. 보시다시피 현재 시스템은 완전히 초기화된 상태입니다. 기존처럼 코딩으로 챗봇을 만드는 것이 아니라, 이 대시보드에서 **클릭 몇 번으로 엔터프라이즈급 챗봇 지식을 구축(Factory)** 하는 과정을 보여드리겠습니다. 먼저 '탄소중립 플랫폼 데모'라는 이름으로 신규 프로젝트를 생성하겠습니다."

---

## 2단계: 지식 자료 등록 (02:00 ~ 05:00)

**[화면 액션]**
- **[지식 관리] > [문서 업로드]** 이동
- 사전에 백업해 둔 `J-Brain_Manual.txt` 와 `03_J-Brain_운영_매뉴얼_및_가이드.md`를 드래그 앤 드롭으로 업로드
- **[문서 파싱 및 GraphRAG 인덱싱 시작]** 클릭 (상태바가 돌아가는 것을 보여줌)

**🗣️ 발표 멘트:**
> "챗봇이 똑똑해지려면 회사의 사내 규정이나 매뉴얼을 학습해야 합니다. 외부 유출 걱정 없이, 사내 매뉴얼 문서 2개를 업로드 해보겠습니다. 지금 백엔드에서는 이 문서들을 의미 단위로 쪼개고(Chunking) 벡터 데이터베이스에 저장하는 GraphRAG 인덱싱 작업이 진행되고 있습니다. 외부 LLM에 문서를 통째로 넘기는 것이 아니라, 내부망에 안전하게 지식 그래프로 구축되는 과정입니다."

*(진행되는 동안 잠시 대기하며)*
> "인덱싱이 완료되었습니다. 이제 이 문서를 바탕으로 챗봇이 어떤 질문을 받을지 의도를 설계해보겠습니다."

---

## 3단계: 의도(Intent) 설계 (05:00 ~ 08:00)

**[화면 액션]**
- **[Intent Factory] > [의도 설계]** 메뉴로 이동
- 시스템이 문서 기반으로 자동 추출해 준 후보군을 닫고, **수동으로 4가지 주요 Action 유형별 Intent를 직접 생성**하는 시연 진행
- **[신규 Intent 생성]** 버튼을 눌러 다음 4개의 의도를 차례로 입력합니다.

**1. [지식 검색용] 매뉴얼 검색 의도**
- **Intent 명:** `매뉴얼_검색`
- **예상 질문(Examples):** "플랫폼 매뉴얼 찾아줘", "사용 가이드 보여줄래?"
- **핵심 개체(Entity) 및 동의어:** `Entity:` "매뉴얼" / `Synonym:` "사용 가이드", "가이드북"

**2. [API 연동용] 사용자 통계 조회 의도**
- **Intent 명:** `사용자_통계_API`
- **예상 질문(Examples):** "J-Brain 현재 접속 사용자 몇 명이야?", "프로젝트 사용자 통계 API로 보여줘"
- **핵심 개체(Entity) 및 동의어:** `Entity:` "사용자 통계" / `Synonym:` "유저 목록", "접속자 수"

**3. [SQL 직접 조회용] 사용자 목록 조회 의도**
- **Intent 명:** `사용자_목록_SQL`
- **예상 질문(Examples):** "DB에 등록된 전체 사용자 목록 조회해줘", "시스템 사용자 명단 쿼리로 가져와"
- **핵심 개체(Entity) 및 동의어:** `Entity:` "전체 사용자" / `Synonym:` "등록 계정", "어드민 명단"

**4. [화면 이동용] 보고서 작성 라우팅 의도**
- **Intent 명:** `보고서_작성_이동`
- **예상 질문(Examples):** "월간 보고서 작성 페이지로 가줘", "보고서 기안 올릴래"
- **핵심 개체(Entity) 및 동의어:** `Entity:` "보고서 기안" / `Synonym:` "결재 작성"

**🗣️ 발표 멘트:**
> "이곳은 챗봇의 두뇌인 '의도(Intent)'를 기획하는 팩토리 공간입니다. 기존의 룰 기반 챗봇과 달리, 사용자가 조금 다르게 말해도 정확히 의도를 매칭해낼 수 있도록 핵심 질문 패턴(Examples)과 동의어 사전을 등록해줍니다. 오늘 시연을 위해 챗봇이 할 수 있는 **4가지 핵심 행동 유형(문서 검색, API 호출, DB 직접 조회, 화면 이동)**에 맞춰 각각의 의도를 직접 만들어 보겠습니다."

---

## 4단계: FAQ & Action 연결 (08:00 ~ 10:00)

**[화면 액션]**
- 방금 만든 4개의 Intent 상세 설정 창에서 각각 **[Action 연결]** 탭을 클릭하여 상세 정보를 입력합니다.

**1. [유형 1: 지식/문서 검색] `매뉴얼_검색` Intent**
- **Action 종류:** `문서/FAQ 검색 (RAG)` 선택
- **검색 대상(Source):** `전체 문서` 또는 업로드한 `J-Brain_Manual.txt` 지정
- **응답 템플릿:** "요청하신 매뉴얼 내용입니다: \n\n {rag_result}"
- *설명: 환각 없이 오직 문서 내에서만 답을 찾도록 제한합니다.*

**2. [유형 2: REST API 연동] `사용자_통계_API` Intent**
- **Action 종류:** `API/SQL Template 실행` 선택
- **연동 방식:** `REST API` 선택
- **API Method & URL:** `GET` / `/api/v1/projects/statistics/users` (프로젝트 통계 API 연동)
- **응답 템플릿:** "현재 J-Brain 프로젝트에 접속중인 통계입니다: \n\n {api_result}"
- *설명: LLM이 데이터를 지어내지 못하게 사내 통계 API를 직접 호출하여 실시간 데이터를 가져옵니다.*

**3. [유형 3: SQL 쿼리 직접 조회] `사용자_목록_SQL` Intent**
- **Action 종류:** `API/SQL Template 실행` 선택
- **연동 방식:** `SQL Query` 선택
- **쿼리 템플릿:** `SELECT email, role FROM admin_users WHERE is_active = true;`
- **응답 템플릿:** "DB에 등록된 활성 사용자 명단입니다: \n\n {sql_result}"
- *설명: 사내 DB에 직접 쿼리를 수행하여 정확한 로우(Row) 데이터를 반환합니다.*

**4. [유형 4: 메뉴/화면 이동] `보고서_작성_이동` Intent**
- **Action 종류:** `지정 URL 이동 (Route)` 선택
- **이동 경로(URL):** `/report/write`
- **응답 템플릿:** "네, 월간 보고서 기안 페이지로 안내해 드리겠습니다."
- *설명: 사용자가 복잡한 뎁스의 메뉴를 찾을 필요 없이 바로 목적지로 라우팅합니다.*

**🗣️ 발표 멘트:**
> "의도를 파악했다면, 챗봇이 '어떤 행동(Action)'을 해야 할지 지정해야 합니다. J-Brain의 가장 큰 차별점이 바로 이 부분입니다. 단순히 생성형 AI가 그럴싸한 답변을 지어내는(환각) 것이 아니라 완벽하게 통제된 4가지 행동을 수행합니다."
> "RAG를 활용한 **문서 검색**, 사내 시스템과 통신하는 **REST API 호출**, 데이터베이스에서 즉각 데이터를 뽑아오는 **SQL 쿼리 실행**, 그리고 사용자를 목적지로 보내주는 **URL 라우팅** 입니다. 각각의 의도에 맞게 이 4가지 액션을 맵핑했습니다."

---

## 5단계: Pack 검증 / 빌드 (10:00 ~ 12:00)

**[화면 액션]**
- **[배포 패키지 관리]** 메뉴 이동
- **[신규 Pack 빌드]** 버튼 클릭
- 빌드 완료 후 `Pack v0.1.0` 생성된 것 확인

**🗣️ 발표 멘트:**
> "지식과 행동 정의가 끝났습니다. 이제 이 챗봇 지식을 배포용 패키지로 묶어보겠습니다. J-Brain은 챗봇 엔진과 지식(Pack)이 완전히 분리되어 있습니다. 이 빌드된 팩을 다운로드해서 실제 폐쇄망 서버에 밀어 넣기만 하면 즉시 챗봇이 구동됩니다."

---

## 6단계: Runtime 시뮬레이션 (12:00 ~ 15:00)

**[화면 액션]**
- **[QA / 시뮬레이션]** 메뉴 이동 및 우측 하단 챗봇 위젯 열기
- **테스트 1 (RAG 검색):** "사용 가이드 좀 보여줄래?" ➡️ 매뉴얼 요약 답변 출력
- **테스트 2 (API 호출):** "현재 사용자 통계 보여줘" ➡️ API를 통한 통계 데이터 출력
- **테스트 3 (SQL 쿼리):** "DB에 등록된 사용자 명단 가져와" ➡️ 실시간 DB 쿼리 기반 이메일/권한 표 출력
- **테스트 4 (화면 이동):** "월간 보고서 기안할래" ➡️ (화면 이동 Action 발생) 보고서 작성 페이지로 라우팅 알림

**🗣️ 발표 멘트:**
> "마지막으로 시뮬레이션 창에서 방금 만든 지식이 어떻게 동작하는지 보겠습니다. '가이드를 보여달라'고 하면 문서를 찾아 요약해줍니다."
> "그리고 가장 중요한 데이터 연동입니다. '통계를 보여달라'고 하면 API를 찔러 데이터를 가져오고, '명단을 가져와'라고 하면 실시간으로 DB SQL을 수행해 정확한 사용자 리스트를 보여줍니다. 모두 LLM의 환각이 개입할 수 없는 100% 팩트 기반 응답이죠."
> "마지막으로 '보고서를 기안하겠다'고 하면 환각 답변을 하는 대신, 즉시 보고서 작성 시스템 화면으로 라우팅 시켜줍니다."

> "이로써 외부망이나 보안 위험 없이, 오직 승인된 절차(Action)대로 완벽히 동작하는 엔터프라이즈 챗봇 구축 시연을 마치겠습니다. 감사합니다!"

---

## [데모 종료 후 조치사항]
- 질의응답 진행 (보조 진행자가 백엔드 로그 모니터링)
- 다음 시연을 대비해야 할 경우, 터미널에서 `python backend/cleanse_demo_data.py` 실행하여 재초기화
