#!/usr/bin/expect -f
set timeout 30
set password "app1234!"
spawn ssh -p 2222 appuser@dev.jwinpartners.com "cd /app/j-brain && git remote -v && git status"
expect {
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "$password\r"
    }
}
expect eof
